services:
  - type: web
    name: ojt-ecommerce-website
    runtime: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: bash start_render.sh
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "localhost,127.0.0.1,.onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: fitness-hub-db
          property: connectionString
      - key: EMAIL_VERIFICATION_REQUIRED
        value: "False"
      - key: BASE_URL
        value: "https://ojt-ecommerce-website.onrender.com"
      - key: SMS_PROVIDER
        value: "console"

databases:
  - name: fitness-hub-db
    plan: free
